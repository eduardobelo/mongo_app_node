
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}
<h1>Nova categoria</h1>
<div class="card mt-4">
    <div class="card-body">
        <form action="/admin/categorias/cadastrada" method="post">
            <div class="form-group">
                <label for="nome">Nome: </label>
                <input type="text" name="nome" id="nome" placeholder="Nome categoria" class="form-control">
            </div>
            <div class="form-group">
                <label for="slug">Nome: </label>
                <input type="text" name="slug" id="slug" placeholder="Slug categoria" class="form-control">
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-success">Criar categoria</button>
            </div>
        </form>
    </div>
</div>